# User-supplied ROM

Place the supported ROM here as:

```text
Tetris (JUE) (V1.1) [!].gb
```

The program accepts only the 32 KiB World revision 1.1 image with SHA-1
`74591cc9501af93873f9a5d3eb12da12c0723bbc`. ROM files are ignored by Git.
You may also pass a different local path with `--rom`.
